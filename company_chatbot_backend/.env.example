# Example environment variables for Company Chatbot Backend
# Copy this file to .env and set appropriate values.

# SECURITY
SECRET_KEY=change-me-in-production

# DATABASE (optional for demo; routes default to in-memory)
# POSTGRES_URL=postgresql+psycopg://user:password@host:5432/dbname

# SERVER
# PORT is read by src/api/server.py when using `python -m src.api.server`
PORT=3001

# CORS
# Comma-separated list of origins, or use ["*"] to allow all in previews
# For pydantic BaseSettings List parsing, set as JSON-style or comma-separated depending on your runner.
# Example JSON-style: CORS_ALLOW_ORIGINS=["*"]
# Example comma-separated: CORS_ALLOW_ORIGINS=*
CORS_ALLOW_ORIGINS=["*"]

# HOSTS (TrustedHostMiddleware)
# IMPORTANT: For non-production and preview environments set:
# ALLOWED_HOSTS=["*"]
# This disables strict host checking to avoid "Invalid Host header" errors on preview domains.
# In production, set explicit hosts, e.g.:
# ALLOWED_HOSTS=["api.example.com","localhost","127.0.0.1"]
ALLOWED_HOSTS=["*"]

# EMBEDDINGS / RAG
EMBEDDING_DIM=384
