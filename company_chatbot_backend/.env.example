# Backend Environment Example
# Copy to .env and provide real values. The orchestrator will inject actual secrets.

# App
APP_NAME=Company Chatbot Backend
APP_DESCRIPTION=Ocean Professional themed API providing chat, authentication, and RAG+FAISS semantic retrieval.
APP_VERSION=0.1.0

# Security
SECRET_KEY=replace_me_with_secure_random
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS
CORS_ALLOW_ORIGINS=["*"]

# Database (SQLAlchemy URL)
# Example: postgresql+psycopg://user:password@host:5432/dbname
POSTGRES_URL=postgresql+psycopg://user:password@localhost:5432/company_chatbot

# Embeddings
EMBEDDING_DIM=384
