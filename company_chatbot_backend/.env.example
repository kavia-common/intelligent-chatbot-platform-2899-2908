# Example environment variables for Company Chatbot Backend

# Application
APP_NAME=Company Chatbot Backend
APP_DESCRIPTION=Ocean Professional themed API providing chat, authentication, and RAG+FAISS semantic retrieval.
APP_VERSION=0.1.0

# Security
# IMPORTANT: Set a secure random value in real deployments
SECRET_KEY=replace-with-a-secure-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Database (optional for demo; required for persistence)
# Example: postgresql+psycopg://user:password@host:5432/dbname
POSTGRES_URL=

# CORS: comma-separated list of allowed origins (use * for any)
# Example: http://localhost:3000,https://your-frontend.example.com
CORS_ALLOW_ORIGINS=*

# Allowed hosts for Starlette TrustedHostMiddleware (comma-separated)
# Defaults allow localhost/0.0.0.0 and wildcard for container preview hosts
# Example: localhost,127.0.0.1,0.0.0.0,*.cloud.kavia.ai
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,*

# Embedding dimension (demo)
EMBEDDING_DIM=384

# Server port (used by src.api.server)
PORT=3001
